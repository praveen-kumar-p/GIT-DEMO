# s = 'hello'
# print(sorted(s))

l = [5, 3, 2, 1, 7, 9, 2]
# print(sorted(l))

# l = ['python', 10, 2+5j, True]
# print(sorted(l))
# s = {'gmail': 5, 'apple':3, 'walmart': 7, 'flipkart': 8}
# print(sorted(s.items(), key = lambda item: item[0][-1]))

# s = {'gmail': 5, 'apple': 3, 'walmart': 7, 'flipkart': 8}
# print(sorted(s.items(), key= lambda item: item[-1]))

# s = 'python is a language python'
# l = s.split()
# shortest, *rest, longest = sorted(l, key=len)
# print((longest, len(longest)), (shortest, len(shortest)))

# a = map(lambda x: x % 2 == 0, a)
# print(a(10))

# a, *, b = [1, 2, 3, 4]
# print(a, *, b)

# a, *z, b = (1, 2, 3)
# print(a, z, b)

# a, _, b = [1, 2, 3, 4]
# print(a, _, b)

# a = lambda x: x*2
# print(a((1, 2)))

# a = map(lambda x: x ** 2, [1, 2, 3, 4])
# print(list(a))

# a = lambda x, y: x*2, y+3
# print(a(1, 2))

# a = map(lambda x: x%2 == 0, [1, 2, 3, 4])
# print(list(a))

# a = filter(lambda x: x ** 2, [1, 2, 3, 4])
# print(list(a))

# path = r'C:\Users\admin\PycharmProjects\Alpha_3\files_directory\csv_files\vaccination_data.csv'
# import csv
# with open(path) as csv_file:
#     read_obj = csv.reader(csv_file)
#     next(read_obj)
#     for row in read_obj:
#         print(row[6])

# path = r'C:\Users\admin\PycharmProjects\Alpha_3\files_directory\csv_files\vaccination_data.csv'
# import csv
# with open(path) as csv_file:
#     read_obj = csv.reader(csv_file)
#     next(read_obj)
#     for row in read_obj:
#         print(row[0])


